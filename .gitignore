node_modules/
.next/
out/
dist/

# env files — NEVER commit these
.env
.env.local
.env.*.local

# Vercel
.vercel

# OS
.DS_Store
Thumbs.db

# logs
*.log
npm-debug.log*

# Playwright
test-results/
playwright-report/

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Sentry — DSN is public (Sentry's DSNs are identifiers, not credentials),
# so we commit the config files so Vercel can find them at build time.
# Only the auth-token file stays out of git.
.env.sentry-build-plugin
