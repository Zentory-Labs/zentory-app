{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "git": {
    "deploymentEnabled": {
      "main": true,
      "master": false
    }
  },
  "ignoreCommand": "if echo \"$VERCEL_GIT_COMMIT_REF\" | grep -q '^data/ledger-'; then exit 0; else exit 1; fi",
  "regions": ["iad1"]
}
